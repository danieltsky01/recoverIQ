{
  "name": "hospital-scorer-workspace",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -k -n \"API,WEB\" -c \"magenta.bold,cyan.bold\" \"npm run dev --prefix functions\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix functions && npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
